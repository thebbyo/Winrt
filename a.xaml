#pragma once
#include "HomeViewModel.g.h"

namespace winrt::Episode4::implementation
{
    struct HomeViewModel : HomeViewModelT<HomeViewModel>
    {
    private:
        winrt::hstring m_title{ L"Samsung Notes" };
        int32_t m_totalNotes{ 12 };
        int32_t m_pinnedNotes{ 3 };
        winrt::hstring m_recentNote{ L"Meeting notes" };

    public:
        HomeViewModel() = default;

        winrt::hstring Title();
        int32_t TotalNotes();
        int32_t PinnedNotes();
        winrt::hstring RecentNote();

        void Refresh();
        void CreateNote();
    };
}

namespace winrt::Episode4::factory_implementation
{
    struct HomeViewModel : HomeViewModelT<HomeViewModel, implementation::HomeViewModel>
    {
    };
}