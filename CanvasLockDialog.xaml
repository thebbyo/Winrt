<UserControl
    x:Class="SamsungNotes.UI.Controls.CanvasLockDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SamsungNotes.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Center"
    VerticalAlignment="Center">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <SolidColorBrush x:Key="CanvasLockPopUpBackgroundColor" Color="#D6D6D6" />
                    <SolidColorBrush x:Key="TitleTextColor" Color="#252528" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Main container with border and background -->
    <Border Background="{ThemeResource CanvasLockPopUpBackgroundColor}" 
            BorderBrush="#66c9c9cc" 
            BorderThickness="0.5" 
            CornerRadius="16"
            Width="180"
            Height="220"
            FlowDirection="LeftToRight"
            >

        <!-- Content Grid with padding -->
        <Grid Margin="0,10,0,0" Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image x:Name="HelpCanvasLockIcon"
                   Grid.Row="0"

                   Source="/SamsungNotes.UI.Controls/Assets/help_CanvasLock.svg"

                   Height="72"   
                   Width="80"  
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center" />

            <TextBlock x:Name="Message"
                   FontSize="16"
                   Grid.Row="1"
                   Width="125"
                   FontWeight="Normal"
                   Margin="0,20,0,24"
                   HorizontalAlignment="Center"
                   Foreground="{ThemeResource TitleTextColor}"
                   TextWrapping="WrapWholeWords"
                   Text="Canvas Locked. Swipe with Two Fingers to Scroll." />
        </Grid>
    </Border>
</UserControl>
