<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="floatingToolBar.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:floatingToolBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="floatingToolBar">

    <Grid>
        <Canvas x:Name="RootCanvas">
            <Border x:Name="FloatingController"
                   Height="Auto"
                   Canvas.Left="20"
                   Canvas.Top="20"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   BorderBrush="#4e4e4e"
                   BorderThickness=".5"
                   Background="#fffcfcfc"
                   CornerRadius="18"
                   >
                <StackPanel Orientation="Horizontal" Spacing="18">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="36" />
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Image x:Name="DragHandle"
                           Grid.Column="0"
                           Source="/D:/SamsungNotes/SamsungNotes/Assets/Images/Composer/Voice/Voice_Recording/ic_voice_floating_handler.svg"
                           Height="36"
                           Width="20"
                           Margin="0,0,0,0"
                           ManipulationMode="TranslateX,TranslateY" 
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           PointerPressed="DragHandle_PointerPressed"
                           PointerMoved="DragHandle_PointerMoved"
                           PointerReleased="DragHandle_PointerReleased"/>
                        <Button Content="a" Grid.Column="1"/>
                        <Button Content="b" Grid.Column="2"/>
                        <Button Content="c" Grid.Column="3"/>

                    </Grid>


                </StackPanel>
            </Border>




        </Canvas>
    </Grid>
</Window>
